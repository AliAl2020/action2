name: artifact
on: push
 
jobs:

  artifact:
    runs-on: ubuntu-latest
    outputs:
        output1: ${{ steps.publish.scriptfile }}
    steps:
      - name: get Code
        uses: actions/checkout@v3
        
      - name: install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: upload artifacts
        uses: actions/upload-artifact@v3
        with:
            name: dist-files2
            path: dist2

      - name: Publish JS filename
        id: publish
        run: find dist2/assets/*.js -type f -execdir echo 'scriptfile={}' >> $GITHUB_OUTPUT ';'



